### Checkbox

```js
const instance = (
  <Form>
    <FormGroup controlId='checkbox'>
      <Checkbox checked > Checkbox</Checkbox>
    </FormGroup>
  </Form>
);
ReactDOM.render(instance, mountNode);
```