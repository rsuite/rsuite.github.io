const TITLE = 'RSUITE 3 | React Suite';

export const setTitle = title => {
  document.title = `${title} | ${TITLE}`;
};

export const defateTilte = () => {
  document.title = `${TITLE} |  一套 React 的 UI 组件库`;
};
